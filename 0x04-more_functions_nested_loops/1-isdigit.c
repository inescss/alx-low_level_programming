#include "main.h"
/**
 *  -isupper -> this is a function to check upper case alphas
 *  @c: an integer paramer
 *  Return: something but success
 */
int _isdigit(int c)
{
if ((c >= 48) && (c <= 57))
return (1);

return (0);
}
